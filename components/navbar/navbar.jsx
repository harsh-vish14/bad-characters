import React from "react";
import classes from "./navbar.module.scss";
const Navbar = () => {
  return (
    <div className={classes.navbar}>
      <div className={classes.logo}>BC</div>
    </div>
  );
};

export default Navbar;
